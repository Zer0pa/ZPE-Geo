# Public Audit Limits

This repo is a private staged extraction, not a completed public release packet.

## What This Repo Does Prove Right Now

- the staged repo boundary exists and is coherent
- the code, scripts, fixtures, runbooks, and adjudicated proof bundle are assembled under one repo root
- the core-wave evidence bundle still reports eight promoted Geo claims as `PASS`
- the contradiction set is visible instead of hidden

## What This Repo Does Not Prove Yet

- blind-clone installation success
- fresh Phase 5 verification on the staged repo root
- Phase 4.5 performance augmentation
- a cleaned or replayed full-corpus maximalization story
- commercialization closure beyond what is already preserved in the archived bundle

## Operator-Only Material Intentionally Excluded

- `.env`
- `data/external_samples/`
- raw `third_party/`
- outer-shell orchestrator records

Some scripts can still use those operator-only resources if they exist one level above the repo root. Their absence does not invalidate the staged repo boundary; it limits what can be rerun without the outer workspace.

## Evidence Honesty Rule

The archived proof bundle preserves historical generated outputs. Some of those files still contain stale absolute-path references and unresolved max-wave contradictions. Those files are preserved as evidence, not normalized into a cleaner story.
